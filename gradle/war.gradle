apply plugin: "war"

bootWar {
    mainClassName = "io.github.jhipster.application.JhipsterSampleApplication1App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
